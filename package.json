{
  "name": "react-example-codes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "dev:react": "turbo dev --filter=@we/react",
    "dev:ssr": "turbo dev --filter=@we/ssr",
    "dev:api": "turbo dev --filter=@we/api",
    "test:react": "turbo test --filter=@we/react",
    "test:ssr": "turbo test --filter=@we/ssr",
    "test:api": "turbo test --filter=@we/api"
  },
  "dependencies": {
    "@we/api": "workspace:*",
    "@we/react": "workspace:*",
    "@we/shared": "workspace:*",
    "@we/ssr": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@tanstack/eslint-plugin-query": "^5.91.2",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.4.23",
    "globals": "^17.0.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript-eslint": "^8.51.0"
  },
  "packageManager": "pnpm@10.10.0"
}
