services:
  db:
    image: postgres:16
    env_file:
      - ./apps/api/.env
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
